status: confirmed
version: "1.2"
source_revision: "CR-071@cp2-revision-2 + CR-072@0.6.1-package-approved"
source_requirements:
  - docs/product/REQUIREMENTS.md
source_use_cases:
  - docs/product/USE-CASES.md
formal_cp2_status: approved
confirmed_by: "user"
confirmed_at: "2026-08-18T08:02:17Z"
formal_cp2_approval_ref: "process/state/GATE-LEDGER.ndjson#GATE-CR072-CP2-APPROVED-20260818-V1"
revision_history:
  - version: "1.0"
    date: "2026-08-15"
    summary: "MF-1～MF-6 initial 18-scenario baseline"
  - version: "1.1"
    date: "2026-08-15"
    summary: "Incrementally absorbed six CP2 changes_requested deltas without changing scenario IDs"
  - version: "1.2"
    date: "2026-08-18"
    summary: "Added CR-072 0.6.1 package/compiler/closure/cost/SemVer/release/consumer scenarios; CR-071 IDs retained"
scenarios:
  - id: SCN-MF1-01
    title: "合法 Work 初始化执行全生命周期零写预检"
    type: positive
    priority: HIGH
    persona: P-01
    source_refs:
      requirements: [REQ-MF1-01, REQ-MF1-02, REQ-MF1-03]
      use_cases: [UC-WORK-PREFLIGHT]
      stories: [ST-MF1]
    trigger: "维护者准备初始化 Work，但尚未 apply"
    context: "route、OID/preimage、scope、budget 和 typed refs 均合法"
    preconditions: ["只读预检已授权", "目标快照可读"]
    steps:
      - action: "以同一快照分别调用 preflight/apply 的 shared validation core，并捕获写集合摘要"
        expected: "normalized item/decision graph 100% 一致；preflight 返回 PASS、完整 lifecycle 模拟项和 mutation=0，差异仅限 orchestration/presentation"
    evidence_type: automated
    automation_status: planned
    gap_reason: ""
  - id: SCN-MF1-02
    title: "无效 revision/scope/budget 在 apply 前被零写阻断"
    type: negative
    priority: HIGH
    persona: P-01
    source_refs:
      requirements: [REQ-MF1-01, REQ-MF1-02, REQ-MF1-03]
      use_cases: [UC-WORK-PREFLIGHT]
      stories: [ST-N-MF1]
    trigger: "preflight 输入包含 stale revision、未闭合 scope 或超限 budget"
    context: "至少一个 lifecycle 前置项无效"
    preconditions: ["目标 bytes 快照已记录"]
    steps:
      - action: "以同一无效快照执行 preflight/apply shared-core validation 并比较前后摘要"
        expected: "两个入口返回相同 BLOCKED/FAIL item/error graph；preflight 所有目标 bytes 与 ledger/state 均不变"
    evidence_type: automated
    automation_status: planned
    gap_reason: ""
  - id: SCN-MF1-03
    title: "on-touch obligations 缺失时预检拒绝计划"
    type: precheck
    priority: HIGH
    persona: P-02
    source_refs:
      requirements: [REQ-MF1-02]
      use_cases: [UC-WORK-PREFLIGHT]
      stories: [ST-N-MF1]
    trigger: "计划会触碰需同步的治理对象但未声明 obligation"
    context: "主输入合法，on-touch closure 不完整"
    preconditions: ["on-touch policy 可读"]
    steps:
      - action: "执行 preflight obligation scan"
        expected: "列出缺失 obligation 和补齐入口，mutation=0"
    evidence_type: automated
    automation_status: planned
    gap_reason: ""
  - id: SCN-MF2-01
    title: "合法 scope delta 生成 append-only 新 revision"
    type: positive
    priority: HIGH
    persona: P-01
    source_refs:
      requirements: [REQ-MF2-01, REQ-MF2-02, REQ-MF2-03]
      use_cases: [UC-SCOPE-AMENDMENT]
      stories: [ST-MF2]
    trigger: "active Work 需要增加已获授权的读写或检查路径"
    context: "revision/preimage 一致且 delta 可分类"
    preconditions: ["BL-001 revision>1 legal supersession admission 已通过", "predecessor/inventory 可确定接纳", "typed authorization 覆盖 delta", "受影响证据可枚举"]
    steps:
      - action: "plan 后 apply scope-amend"
        expected: "MF-2 admission/E2E 先通过；旧 revision 不变，新 revision 含 delta、重新授权与 invalidation set"
    evidence_type: automated
    automation_status: planned
    gap_reason: ""
  - id: SCN-MF2-02
    title: "stale preimage 或未知路径导致 scope-amend 零写失败"
    type: negative
    priority: HIGH
    persona: P-01
    source_refs:
      requirements: [REQ-MF2-01, REQ-MF2-02, REQ-MF2-03]
      use_cases: [UC-SCOPE-AMENDMENT]
      stories: [ST-N-MF2]
    trigger: "BL-001 supersession admission 未通过，或 apply 时 revision/preimage 已漂移或 delta 含 unknown path"
    context: "plan 快照不可复用"
    preconditions: ["原 revision bytes 摘要已记录"]
    steps:
      - action: "尝试 apply scope-amend"
        expected: "MF-2 实现/E2E 或 apply 被拒绝并给出重新 plan/补齐 predecessor inventory 指引，不产生 partial revision"
    evidence_type: automated
    automation_status: planned
    gap_reason: ""
  - id: SCN-MF2-03
    title: "缺少 typed authorization 时拒绝扩大范围"
    type: permission
    priority: HIGH
    persona: P-02
    source_refs:
      requirements: [REQ-MF2-02, REQ-MF2-03]
      use_cases: [UC-SCOPE-AMENDMENT]
      stories: [ST-N-MF2]
    trigger: "delta 机械合法但授权不覆盖新增操作"
    context: "deny-default 权限边界"
    preconditions: ["当前授权集合可读"]
    steps:
      - action: "执行 amend authorization precheck"
        expected: "fail closed，指出缺少的 typed authorization，mutation=0"
    evidence_type: automated
    automation_status: planned
    gap_reason: ""
  - id: SCN-MF3-01
    title: "typed ref 按 repository role 解析 release/process 对象"
    type: positive
    priority: HIGH
    persona: P-02
    source_refs:
      requirements: [REQ-MF3-01, REQ-MF3-02, REQ-MF3-03]
      use_cases: [UC-TYPED-REFS]
      stories: [ST-MF3]
    trigger: "合同同时引用 release 与 process 对象"
    context: "schema/version 和 role 有效"
    preconditions: ["project route identity 健康"]
    steps:
      - action: "解析并 canonicalize refs"
        expected: "输出明确 repository role、logical namespace 和一致前缀语义"
    evidence_type: automated
    automation_status: planned
    gap_reason: ""
  - id: SCN-MF3-02
    title: "unknown role 或相邻字段前缀矛盾时 fail closed"
    type: negative
    priority: HIGH
    persona: P-03
    source_refs:
      requirements: [REQ-MF3-01, REQ-MF3-02]
      use_cases: [UC-TYPED-REFS]
      stories: [ST-N-MF3]
    trigger: "输入含未知仓角色或相反隐式前缀规则"
    context: "ref 不能唯一解释"
    preconditions: ["ambiguous fixtures 可重放"]
    steps:
      - action: "运行 typed ref contract check"
        expected: "返回明确 error code，不猜 sibling、不默认仓、不 mutation"
    evidence_type: automated
    automation_status: planned
    gap_reason: ""
  - id: SCN-MF3-03
    title: "v1 ref 在兼容期获得确定读取或迁移诊断"
    type: boundary
    priority: HIGH
    persona: P-01
    source_refs:
      requirements: [REQ-MF3-03]
      use_cases: [UC-TYPED-REFS]
      stories: [ST-N-MF3]
    trigger: "reader 接收到迁移期 v1 ref"
    context: "read-old/write-new 兼容窗口"
    preconditions: ["legacy provenance 保留"]
    steps:
      - action: "读取 v1 ref、写入新 ref，并运行 declared-scope residual/ambiguity checks"
        expected: "v1 可确定读取或明确报错；新 writer v1 输出=0、declared-scope v1 residual=0、ambiguous/misread 检出率=100%；连续两个 full-validation 快照 v1 input observed=0 前不得提议 reader 退役"
    evidence_type: automated
    automation_status: planned
    gap_reason: ""
  - id: SCN-MF4-01
    title: "canonical validation policy 唯一决定 full 层执行要求"
    type: positive
    priority: HIGH
    persona: P-02
    source_refs:
      requirements: [REQ-MF4-01, REQ-MF4-02, REQ-MF4-03]
      use_cases: [UC-FULL-REGRESSION-SEMANTICS]
      stories: [ST-MF4]
    trigger: "编排器构造 targeted→compatibility→full 执行计划"
    context: "输入使用 canonical successor policy"
    preconditions: ["validation profile 可读"]
    steps:
      - action: "解释 policy 并生成分层执行计划"
        expected: "语义仅表示默认执行策略，required full 层保留且不存在禁令解释"
    evidence_type: automated
    automation_status: planned
    gap_reason: ""
  - id: SCN-MF4-02
    title: "legacy 字段不能导致 required full 层被静默跳过"
    type: negative
    priority: HIGH
    persona: P-03
    source_refs:
      requirements: [REQ-MF4-01, REQ-MF4-02, REQ-MF4-03]
      use_cases: [UC-FULL-REGRESSION-SEMANTICS]
      stories: [ST-N-MF4]
    trigger: "legacy 字段值可能被误读为 full regression 禁令"
    context: "迁移 reader 处理 v1 profile"
    preconditions: ["profile 其他字段要求 full"]
    steps:
      - action: "归一化 legacy profile 并检查执行层"
        expected: "输出迁移 provenance，仍计划 full 层或明确报错，不静默跳过"
    evidence_type: automated
    automation_status: planned
    gap_reason: ""
  - id: SCN-MF4-03
    title: "canonical 与 legacy 输入产生等价分层决策"
    type: boundary
    priority: HIGH
    persona: P-03
    source_refs:
      requirements: [REQ-MF4-02, REQ-MF4-03]
      use_cases: [UC-FULL-REGRESSION-SEMANTICS]
      stories: [ST-N-MF4]
    trigger: "同一业务意图分别由 canonical/v1 fixture 表达"
    context: "兼容窗口内的等价输入"
    preconditions: ["read-old/write-new 迁移合同可用"]
    steps:
      - action: "比较两类输入的 normalized policy/layer plan，并记录连续 full-validation v1 观测快照"
        expected: "决策等价且 legacy 来源可追踪；writer v1 输出=0、residual=0、ambiguous/misread 检出率=100%；仅连续两个快照 v1 observed=0 后可提议 reader 退役"
    evidence_type: automated
    automation_status: planned
    gap_reason: ""
  - id: SCN-MF5-01
    title: "七类 identity 一致时复用稳定验证层"
    type: positive
    priority: HIGH
    persona: P-03
    source_refs:
      requirements: [REQ-MF5-01, REQ-MF5-02, REQ-MF5-03]
      use_cases: [UC-VALIDATION-REUSE]
      stories: [ST-MF5]
    trigger: "新 Work/revision 请求复用既有 receipt"
    context: "canonical semantic-equivalence matrix 判定 source/profile/command/environment/runner/evidence/provenance 语义等价"
    preconditions: ["receipt/evidence 可验证", "无 FAIL 或 partial mutation", "等价 runner/environment fixture 包含 raw string 或 machine incidental differences"]
    steps:
      - action: "运行 canonical semantic-equivalence identity check"
        expected: "等价 fixture 误拒绝数=0；稳定层 reuse=PASS，receipt 引用原 evidence/provenance 并产生可核验 digest"
    evidence_type: automated
    automation_status: planned
    gap_reason: ""
  - id: SCN-MF5-02
    title: "任一 identity 漂移时拒绝受影响层复用"
    type: negative
    priority: HIGH
    persona: P-03
    source_refs:
      requirements: [REQ-MF5-01, REQ-MF5-02]
      use_cases: [UC-VALIDATION-REUSE]
      stories: [ST-N-MF5]
    trigger: "候选 receipt 的至少一个 identity 与当前执行不一致"
    context: "canonical matrix 中的单层或多层安全相关非等价漂移"
    preconditions: ["current identity 与候选 receipt 均可读"]
    steps:
      - action: "计算差异与失效层"
        expected: "安全相关漂移拒绝率=100%；拒绝受影响层，只计划失效层重跑，不用旧 PASS 掩盖漂移"
    evidence_type: automated
    automation_status: planned
    gap_reason: ""
  - id: SCN-MF5-03
    title: "evidence/provenance 不可读时 receipt fail closed"
    type: external-failure
    priority: HIGH
    persona: P-02
    source_refs:
      requirements: [REQ-MF5-02, REQ-MF5-03]
      use_cases: [UC-VALIDATION-REUSE]
      stories: [ST-N-MF5]
    trigger: "receipt 指向缺失证据或 provenance/digest 不一致"
    context: "证据依赖失败"
    preconditions: ["候选 receipt 本身可解析"]
    steps:
      - action: "请求复用并验证 evidence/provenance"
        expected: "返回 BLOCKED/FAIL 和恢复步骤，receipt 不可复用，安排受影响层重跑"
    evidence_type: automated
    automation_status: planned
    gap_reason: ""
  - id: SCN-MF6-01
    title: "未登记失败使 state/current 同步降级并列出来源"
    type: positive
    priority: HIGH
    persona: P-02
    source_refs:
      requirements: [REQ-MF6-01, REQ-MF6-02, REQ-MF6-03]
      use_cases: [UC-UNREGISTERED-FAILURE-VISIBILITY]
      stories: [ST-MF6]
    trigger: "projection inputs 包含 ledger 外失败或 baseline drift"
    context: "formal truth 可读且失败源存在"
    preconditions: ["state/current renderer 使用同一 source set"]
    steps:
      - action: "重建 state/current projection"
        expected: "两者均列出 failure/source，health/readiness 降级且 next action 可追踪"
    evidence_type: automated
    automation_status: planned
    gap_reason: ""
  - id: SCN-MF6-02
    title: "失败归属未知时 projection fail closed"
    type: negative
    priority: HIGH
    persona: P-03
    source_refs:
      requirements: [REQ-MF6-01, REQ-MF6-02]
      use_cases: [UC-UNREGISTERED-FAILURE-VISIBILITY]
      stories: [ST-N-MF6]
    trigger: "检测到失败但缺少 owner/source 归属证据"
    context: "unknown failure ownership"
    preconditions: ["原 projection 显示健康"]
    steps:
      - action: "执行 projection consistency check"
        expected: "health 降级并要求补证，不创建虚假 owner/绿色状态"
    evidence_type: automated
    automation_status: planned
    gap_reason: ""
  - id: SCN-MF6-03
    title: "补齐有效归属证据后一次 reprojection 退出阻断并收敛"
    type: boundary
    priority: HIGH
    persona: P-03
    source_refs:
      requirements: [REQ-MF6-03]
      use_cases: [UC-UNREGISTERED-FAILURE-VISIBILITY]
      stories: [ST-N-MF6]
    trigger: "SCN-MF6-02 因归属证据不足而阻断后，补齐有效 owner/source evidence"
    context: "两阶段 failure recovery 与稳定输入 no-op"
    preconditions: ["阻断 projection 快照可读", "新增证据通过真实性与归属校验"]
    steps:
      - action: "只执行一次 reprojection，随后以相同 source digest 再投影一次"
        expected: "第一次退出 blocked 且 state/current failure/health facts 收敛；不得手改派生状态；第二次除允许时间字段外语义 mutation=0"
    evidence_type: automated
    automation_status: planned
    gap_reason: ""
  - id: SCN-072-01
    title: "完整 Work A/B Package 被编译为唯一 canonical Plan"
    type: positive
    priority: HIGH
    persona: P-02
    source_refs:
      requirements: [REQ-072-01, REQ-072-02]
      use_cases: [UC-PLAN-COMPILER]
      stories: [ST-072-PLAN]
    trigger: "两个 planned Work 提交 0.6.1 Package 候选"
    context: "manifest、owner、priority、依赖和 public CLI registration 完整"
    preconditions: ["CP2 后才可进入实现；本场景仅定义 planned 验证"]
    steps:
      - action: "编译 Package Plan IR"
        expected: "输出唯一 Package、两 Work 归属、确定排序和 CLI registration evidence；不创建对象"
    evidence_type: automated
    automation_status: planned
    gap_reason: ""
  - id: SCN-072-02
    title: "缺 package completeness、priority、owner 或 public CLI 时 compiler 零写阻断"
    type: negative
    priority: HIGH
    persona: P-03
    source_refs:
      requirements: [REQ-072-01, REQ-072-02, REQ-072-12]
      use_cases: [UC-PLAN-COMPILER]
      stories: [ST-N-072-PLAN]
    trigger: "候选 Package 含缺字段、重复 owner、无效 priority 或未注册 CLI"
    context: "deny-default compiler admission"
    preconditions: ["输入快照可读"]
    steps:
      - action: "运行 compiler precheck"
        expected: "逐项 BLOCKED、指出 owner/recovery，mutation=0，未创建 Work/Story/CP"
    evidence_type: automated
    automation_status: planned
    gap_reason: ""
  - id: SCN-072-03
    title: "direct/transitive closure 和 literal SHA 决定 affected-only rebuild"
    type: positive
    priority: HIGH
    persona: P-01
    source_refs:
      requirements: [REQ-072-03, REQ-072-04]
      use_cases: [UC-CLOSURE-BUILD]
      stories: [ST-072-CLOSURE]
    trigger: "一个已知 root/SHA 发生变化"
    context: "依赖图含 direct 与 transitive 关系"
    preconditions: ["canonical graph 与 package identity 可解析"]
    steps:
      - action: "计算 closure 和 rebuild plan"
        expected: "包含所有受影响 direct/transitive 对象、按 literal SHA 追踪，未受影响对象不进入 build"
    evidence_type: automated
    automation_status: planned
    gap_reason: ""
  - id: SCN-072-04
    title: "缺 graph、非法 SHA 或稳定输入时 closure fail closed/no-op"
    type: boundary
    priority: HIGH
    persona: P-03
    source_refs:
      requirements: [REQ-072-03, REQ-072-04]
      use_cases: [UC-CLOSURE-BUILD]
      stories: [ST-N-072-CLOSURE]
    trigger: "closure 输入不完整、SHA 非 literal 或 fingerprint 未变"
    context: "失败恢复与无影响边界"
    preconditions: ["上次 closure 摘要可读"]
    steps:
      - action: "重算 closure"
        expected: "不完整输入 BLOCKED；稳定输入 affected set 不扩大且语义 no-op"
    evidence_type: automated
    automation_status: planned
    gap_reason: ""
  - id: SCN-072-05
    title: "measure-only 成本报告可追溯且重复计算 no-op"
    type: positive
    priority: HIGH
    persona: P-02
    source_refs:
      requirements: [REQ-072-05]
      use_cases: [UC-PROCESS-COST]
      stories: [ST-072-COST]
    trigger: "Package 生成成本 baseline"
    context: "append-only records 与 receipts 均可验证"
    preconditions: ["policy 处于 measure-only"]
    steps:
      - action: "派生两次 cost report"
        expected: "报告包含来源/digest/count/baseline；第二次除时间字段外语义 no-op"
    evidence_type: automated
    automation_status: planned
    gap_reason: ""
  - id: SCN-072-06
    title: "hard-gate 阶段拒绝超阈值、harness error 或重复 qualification"
    type: negative
    priority: HIGH
    persona: P-03
    source_refs:
      requirements: [REQ-072-05, REQ-072-06, REQ-072-10]
      use_cases: [UC-PROCESS-COST, UC-RELEASE-ORDER]
      stories: [ST-N-072-COST]
    trigger: "hard-gate 后成本超阈值、CHECK_HARNESS_ERROR 未关闭或 qualification count>1"
    context: "不以 harness error 伪装内容失败"
    preconditions: ["批准的 hard-gate policy 可读"]
    steps:
      - action: "执行 admission 判断"
        expected: "明确标记 CHECK_HARNESS_ERROR 或 BLOCKED、列恢复条件，禁止 qualification/release"
    evidence_type: automated
    automation_status: planned
    gap_reason: ""
  - id: SCN-072-07
    title: "真实 SemVer 分类后一次 bootstrap 决定 0.6.1"
    type: positive
    priority: HIGH
    persona: P-02
    source_refs:
      requirements: [REQ-072-07, REQ-072-08]
      use_cases: [UC-SEMVER-DECISION]
      stories: [ST-072-SEMVER]
    trigger: "source freeze 前出现 non-breaking minor/0.7.0 classifier 建议"
    context: "typed bootstrap 为 reusable=false 且尚未消费"
    preconditions: ["兼容性证据可审计"]
    steps:
      - action: "执行 classifier 后请求 bootstrap"
        expected: "保留真实分类理由并只生成一次 0.6.1 decision；不是 PATCH 伪装"
    evidence_type: automated
    automation_status: planned
    gap_reason: ""
  - id: SCN-072-08
    title: "breaking change 或 bootstrap 重用被 SemVer 门阻断"
    type: negative
    priority: HIGH
    persona: P-03
    source_refs:
      requirements: [REQ-072-07, REQ-072-08]
      use_cases: [UC-SEMVER-DECISION]
      stories: [ST-N-072-SEMVER]
    trigger: "分类器检测 breaking change，或同一 token 被第二次/跨版本使用"
    context: "兼容边界保护"
    preconditions: ["分类或 token provenance 可读"]
    steps:
      - action: "请求版本决策"
        expected: "BLOCKED，保留真实分类，不能选择 0.6.1/patch 作为绕过"
    evidence_type: automated
    automation_status: planned
    gap_reason: ""
  - id: SCN-072-09
    title: "严格 release-order 形成一次 aggregate qualification/build/canary 血缘"
    type: positive
    priority: HIGH
    persona: P-02
    source_refs:
      requirements: [REQ-072-09, REQ-072-10]
      use_cases: [UC-RELEASE-ORDER]
      stories: [ST-072-RELEASE]
    trigger: "两个 Work 都满足聚合候选完成条件"
    context: "唯一 0.6.1 Package"
    preconditions: ["source freeze 与 version decision 已验证"]
    steps:
      - action: "规划 freeze→decision→fingerprint→qualification→build→canary→tag/release"
        expected: "每步只有一次且同一 lineage；本阶段不执行这些动作"
    evidence_type: manual
    automation_status: planned
    gap_reason: "需要后续独立运行/发布授权"
  - id: SCN-072-10
    title: "freeze drift、倒序或 Work A 单独发布只失效受影响步骤"
    type: negative
    priority: HIGH
    persona: P-03
    source_refs:
      requirements: [REQ-072-09, REQ-072-10, REQ-072-12]
      use_cases: [UC-RELEASE-ORDER]
      stories: [ST-N-072-RELEASE]
    trigger: "freeze 后 fingerprint 漂移、步骤倒序或 Work A 请求中间 receipt/release"
    context: "一次最终发布血缘"
    preconditions: ["先前步骤摘要可读"]
    steps:
      - action: "评估 transition"
        expected: "BLOCKED，只失效 drift 影响步骤；不生成中间版本、qualification、build、canary 或 release"
    evidence_type: automated
    automation_status: planned
    gap_reason: ""
  - id: SCN-072-11
    title: "clean-home published-asset canary 只消费已构建 Package"
    type: positive
    priority: HIGH
    persona: P-01
    source_refs:
      requirements: [REQ-072-11]
      use_cases: [UC-PUBLISHED-ASSET-CONSUMER]
      stories: [ST-072-CONSUMER]
    trigger: "最终 build 后获得独立 canary 授权"
    context: "隔离 clean-home 与 published asset"
    preconditions: ["后续授权可用；本轮仅验证 contract"]
    steps:
      - action: "由 clean-home 调用 public CLI"
        expected: "仅依赖 published asset，验证 package completeness/CLI；不把 source tree 作为回退"
    evidence_type: manual
    automation_status: planned
    gap_reason: "CP2 不授权真实安装或运行"
  - id: SCN-072-12
    title: "缺 asset、CLI、隔离性或授权时 consumer canary fail closed"
    type: permission
    priority: HIGH
    persona: P-03
    source_refs:
      requirements: [REQ-072-11, REQ-072-12]
      use_cases: [UC-PUBLISHED-ASSET-CONSUMER]
      stories: [ST-N-072-CONSUMER]
    trigger: "资产不完整、CLI 未注册、home 污染或无安装/运行授权"
    context: "消费者完整性与权限边界"
    preconditions: ["canary 计划已定义"]
    steps:
      - action: "执行前置检查或后续授权 canary"
        expected: "BLOCKED/FAIL 并给出恢复入口；不读取凭据、不真实安装、不降级到源树"
    evidence_type: automated
    automation_status: planned
    gap_reason: ""
coverage_summary:
  total: 30
  by_type:
    positive: 11
    negative: 11
    boundary: 4
    permission: 2
    empty-data: 0
    external-failure: 1
    precheck: 1
  high_priority: 30
  high_priority_positive_and_negative_covered: true
  requirement_refs_total: 30
  requirement_refs_covered: 30
  use_case_refs_total: 12
  use_case_refs_covered: 12
  gaps: []
  review_deltas_covered: [REV-01, REV-02, REV-03, REV-04, REV-05, REV-06]
