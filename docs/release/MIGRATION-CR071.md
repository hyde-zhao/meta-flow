# CR-071 Migration and Compatibility (CP8 candidate snapshot)

The final publication contract is `MIGRATION-0.5.3.md`; this file preserves the earlier CP8 input.

No migration is executed in CP8 preparation.

| Surface | Assessment | Required handling |
|---|---|---|
| State/projection schema | Conditional typed correction lineage/effective projection | Preserve raw history; validate preimages |
| Templates/configuration | No blanket rewrite authorized | Consume declared versioned contracts |
| Installation paths | No change asserted by this documentation slice | Platform dry-run before install |
| Agent frontmatter/Skill output | CP7 compatibility evidence pending | Bind aggregate evidence before readiness |
| Commands | Additive preflight and scope-amend contracts | Unknown/stale refs fail closed |
| Data structures | Observation, receipt, recovery, lineage schemas | Bind version and digest |

Reader retirement is not CR-071 completion; it requires observation continuity and a later formal gate.
