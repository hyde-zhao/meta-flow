# meta-dm Archive

Status: DEPRECATED

`meta-dm` was the historical Story decomposition and wave planning agent. Its responsibilities have been merged into `meta-se`.

Dispatch policy: 不得唤醒 / no-dispatch. Do not install this agent into Codex, Claude Code, Qoder, or any other target platform. Keep this file only as an audit pointer for older workflow records that mention `meta-dm`.

Replacement:
- Story decomposition: `meta-se`
- Wave planning: `meta-se` with `wave-planner`
- Dependency mapping: `meta-se` with `dependency-mapper`
