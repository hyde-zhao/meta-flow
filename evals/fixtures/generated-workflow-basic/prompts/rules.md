# Generated Workflow Rules

- Use the declared workflow manifest as the routing source.
- Keep dry-run and review-only checks separate from real execution.
- Do not request credentials, production writes, publish actions, or live operations.
- Record case id, grader id, artifact hash, and prompt bundle hash in run evidence.
