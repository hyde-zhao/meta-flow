---
name: data-design
description: >-
  D-Data 等价类+边界值法用例设计：四步完成等价类划分→边界值选取→逻辑用例→物理用例。
  基于 PPDCS 中 D-Data 特征：数据有取值范围，各数据项相对独立。
  触发词包括：等价类、边界值、数据分析、D-Data。
  适用场景：MFQ 设计阶段，PPDCS 特征为 D-Data 的逻辑用例。
argument-hint: "逻辑用例 ID（如 LC-004）"
user-invokable: true
status: active
---

## 目标

对设计计划中 PPDCS 特征为 **D-Data** 的逻辑用例，执行四步设计过程，
等价类划分→边界值选取→逻辑用例→输出物理用例。

## 理论基础

D-Data 是 PPDCS 五特征之一：
> 被测功能的输入数据有明确的取值范围，各数据项相对独立，
> 每个数据项可单独验证有效性。

**识别条件**：需求描述中包含"取值范围"、"有效值"、"最大最小"等词，
且各参数之间没有业务规则依赖。

**关键区分**：
- D-Data vs P-Parameter — 参数间有规则？有 = Parameter，无/独立 = Data
- D-Data vs C-Combination — 独立验证够？够 = Data，需交互组合 = Combination

**建模工具**：等价类划分表 + 边界值分析

## 适用范围

- 适用阶段：MFQ 的 design 阶段
- 输入：`.output/integration/design-plan.md`（PPDCS=D-Data 的 LC）
- 输出：`.output/design/<module>/<sub-module>/` 目录下的设计文件

## 前置条件

- [ ] 设计计划已确认
- [ ] 当前逻辑用例的 PPDCS 特征为 D-Data

## 五步用例设计过程

读取 `test-point-integrator` 输出的逻辑用例（含因子-取值表和动作路径），执行以下五步：

### 第一步：测试数据（因子-取值表补全 + 等价类细化）

从整合阶段的因子-取值表出发，补充因子类型，并细化等价类（有效/边界/无效）：

```markdown
| 因子 | 取值 | 类型 | 等价类 |
|------|------|------|--------|
| 日志保存天数 | 1 | 参数 | 有效（最小边界） |
| 日志保存天数 | 2 | 参数 | 有效（最小+1） |
| 日志保存天数 | 30 | 参数 | 有效（典型值） |
| 日志保存天数 | 364 | 参数 | 有效（最大-1） |
| 日志保存天数 | 365 | 参数 | 有效（最大边界） |
| 日志保存天数 | 0 | 参数 | 无效（最小-1） |
| 日志保存天数 | 366 | 参数 | 无效（最大+1） |
| 日志保存天数 | -1 | 参数 | 无效（负数） |
| 日志保存天数 | 1.5 | 参数 | 无效（非整数） |
| 日志保存天数 | 空 | 参数 | 无效（空值） |
| 日志文件大小(MB) | 1 | 参数 | 有效（最小边界） |
| 日志文件大小(MB) | 1024 | 参数 | 有效（最大边界） |
| 日志文件大小(MB) | 0 | 参数 | 无效（最小-1） |
| 日志文件大小(MB) | 1025 | 参数 | 无效（最大+1） |
```

**边界值选取规则**：`min, min+1, 典型值, max-1, max`（有效区5点）+ `min-1, max+1`（无效边界2点）+ 特殊值（0, 空, null, 负数, 浮点数）

### 第二步：动作路径（D-Data 通常单一路径）

**D-Data 的操作动作独立于数据取值**，通常为 1 条路径：

```
路径 P1（唯一路径）：
1. 进入日志配置 → 日志保存设置页面
2. 将目标参数修改为测试数据值
3. 点击"保存"
4. 刷新页面，检查保存结果
```

若多个数据项的配置页面不同，可枚举多条路径（每个数据项一条），但同一路径不做组合。

### 第三步：数据组合分析（D-Data 等价类策略）

**D-Data 的组合策略**：各数据项**独立验证**，不做跨数据项组合（与 C-Combination 的核心区别）

| 策略类型 | 适用条件 | 说明 |
|---------|---------|------|
| **有效值合并** | 有效等价类 | 多个有效数据项可在同一条用例中配置 |
| **无效值隔离** | 无效等价类 | 每次只测一个无效值，其余为有效值 |
| **边界值优先** | 等价类边界 | 优先测试 min, max, min-1, max+1 |

**组合约束**（D-Data 通常无交叉约束，但若有则记录）：
```
无约束（各数据项完全独立）
```

**全量组合结果**（等价类驱动，非全排列）：

| 组合编号 | 日志保存天数 | 日志文件大小 | 等价类说明 | 预期结果 |
|---------|------------|------------|----------|---------|
| C-01 | 30 | 512 | 全有效（典型值组合） | 配置成功 |
| C-02 | 1 | 512 | 保存天数最小边界 | 配置成功 |
| C-03 | 365 | 512 | 保存天数最大边界 | 配置成功 |
| C-04 | 0 | 512 | 保存天数=0（无效，隔离） | 提示输入无效 |
| C-05 | 366 | 512 | 保存天数超上限（无效，隔离） | 提示超出范围 |
| C-06 | -1 | 512 | 保存天数负数（无效，隔离） | 提示输入无效 |
| C-07 | 空 | 512 | 保存天数为空（无效，隔离） | 提示不能为空 |
| C-08 | 30 | 1 | 文件大小最小边界 | 配置成功 |
| C-09 | 30 | 0 | 文件大小=0（无效，隔离） | 提示输入无效 |

### 第四步：数据×路径组合

**D-Data 覆盖策略**：BA组合（有效值合并，无效值各自独立）

```
最终用例集：
- 有效等价类用例：C-01（典型有效）+ C-02（min边界）+ C-03（max边界）+ C-08（另一因子min）
- 无效等价类用例：C-04~C-07（保存天数各无效类各一条）+ C-09（文件大小无效）
决策理由：D-Data 独立验证，无效值必须隔离，有效值可合并；BA组合平衡效率
```

### 第五步：物理用例输出

```markdown
| 三级目录 | 四级目录 | 五级目录 | 用例名称* | 用例编号 | 用例级别* | 组网描述* | 组网约束 | 预置条件 | 测试步骤* | 预期结果* | 首次创建版本* | 最后变更版本 | 关键词 | 测试类型* | 是否自动化* |
|---------|---------|---------|---------|---------|---------|---------|---------|---------|---------|---------|------------|------------|--------|---------|----------|
| 日志中心 | 配置管理 | 日志备份配置 | 日志保存天数最小值配置 | PC-CFG-DAT-001 | P2 | 单台防火墙 | | 防火墙已正常启动；管理员已登录 | 1.进入日志配置→日志保存设置<br>2.将保存天数修改为 1<br>3.点击"保存"<br>4.刷新页面 | 1.显示保存设置页面<br>2.输入框接受输入<br>3.提示"配置成功"<br>4.保存天数显示为 1 | V60R001C01 | | 日志保存,边界值 | 功能 | 否 |
| 日志中心 | 配置管理 | 日志备份配置 | 日志保存天数超上限 | PC-CFG-DAT-005 | P2 | 单台防火墙 | | 防火墙已正常启动；管理员已登录 | 1.进入日志配置→日志保存设置<br>2.将保存天数修改为 366<br>3.点击"保存" | 1.显示保存设置页面<br>2.输入框接受输入<br>3.提示"保存天数超出范围，请输入 1-365 的整数" | V60R001C01 | | 日志保存,无效输入 | 功能 | 否 |
```

## 输出目录结构

```
.output/design/<module>/<sub-module>/
├── ppdcs-profile.md      # D-Data 特征详情（含等价类统计）
├── design-process.md      # 五步设计过程（因子-等价类表+路径+组合分析+覆盖策略）
└── physical-cases.md      # 物理用例列表
```

### ppdcs-profile.md 内容

```markdown
# PPDCS 特征详情

- **主特征**：D-Data
- **判定依据**：<数据有取值范围，各项独立>
- **辅特征**：<如有>
- **数据项数**：N
- **有效等价类数**：M
- **无效等价类数**：K
- **边界值数**：J
- **预估用例数**：L
```

## 优先级分配规则

| 用例类型 | 优先级 |
|---------|--------|
| 有效典型值 | P1 |
| 有效边界值（min, max） | P2 |
| 无效边界值（min-1, max+1） | P2 |
| 类型错误（非整数、空值） | P3 |
| 极端值 | P3~P4 |

## D-Data vs C-Combination 的设计差异

| 维度 | D-Data | C-Combination |
|------|--------|---------------|
| 因子关系 | 独立 | 需交互 |
| 验证方式 | 每个数据项单独验证 | 多因子组合验证 |
| 无效值测试 | 一次一个无效值 | 按组合策略 |
| 压缩需求 | 无（线性增长） | 需 Pairwise/正交压缩 |

## Gotchas

- D-Data 的核心是"独立性"：各数据项可单独验证，不需要组合
- 无效等价类必须隔离测试：一次只测一个无效值
- 如果发现某些数据项之间有规则依赖 → 应回退到 P-Parameter
- 如果发现需要组合测试 → 应回退到 C-Combination
- 边界值分析的"5+2"法则：min, min+1, typical, max-1, max（有效区）+ min-1, max+1（无效区）

## 验收标准

- [ ] 第一步每个数据项均有完整等价类划分（有效/边界/无效），含类型标注
- [ ] 边界值覆盖：min, min+1, 典型值, max-1, max（有效）+ min-1, max+1（无效）
- [ ] 第二步动作路径通常为 1 条（数据项独立于操作路径）
- [ ] 第三步无效等价类用例必须隔离（每次只设一个无效值）
- [ ] 第四步覆盖策略：有效值合并 + 无效值独立
- [ ] 物理用例以表格输出（16列），C→预置条件、A→测试步骤、E→预期结果映射正确
- [ ] `ppdcs-profile.md` 已创建
- [ ] 设计过程文档写入 `.output/design/<module>/<sub>/`
