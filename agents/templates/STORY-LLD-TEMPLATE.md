---
story_id: "STORY-{id}"
title: ""
lld_version: "1.0"
status: "draft"           # draft | ready-for-lld-review | lld-approved
created_by: "meta-dev"
created_at: ""
confirmed_by: ""
confirmed_at: ""
---

# LLD: STORY-{id} — {标题}

> 本文档是 `STORY-{id}` 的低层设计（Low-Level Design），需经人工确认后方可进入实现。

---

## 1. 实现目标

<!-- 一句话：本 Story 要创建/修改/删除什么，达成什么效果 -->

---

## 2. 文件影响范围

> 使用确定性动词（创建/修改/删除），不允许"可能"、"也许"等模糊表述。

| 操作 | 文件路径 | 变更描述 |
|------|---------|---------|
| 创建 | `.output/skills/<name>/SKILL.md` | 新建 Skill 定义 |
| 修改 | `.output/agents/ptm-tde.md` | 更新状态机映射表 |
| 删除 | — | — |

---

## 3. 数据模型 / 接口设计

### 3.1 输入格式

```
输入文件：<路径>
字段说明：
  - field_a: <类型> — <说明>
  - field_b: <类型> — <说明>
```

### 3.2 输出格式

```markdown
# <输出文件标题>

| 字段A | 字段B | 字段C |
|------|------|------|
| ...  | ...  | ...  |
```

### 3.3 关键 Frontmatter 字段

| 文件 | 字段 | 类型 | 必填 | 取值范围 |
|------|------|------|------|---------|
| SKILL.md | name | string | ✅ | kebab-case |
| SKILL.md | version | string | ✅ | semver |
| SKILL.md | status | string | ✅ | active / draft |

---

## 4. 核心逻辑设计

### 步骤 1: {步骤名}

<!-- 描述该步骤的处理逻辑，使用伪代码或列表 -->

```
输入: <来源>
处理:
  1. 读取 ...
  2. 判断 ...
  3. 输出 ...
输出: <目标>
```

### 步骤 2: {步骤名}

<!-- 同上 -->

---

## 5. 异常处理

| 异常场景 | 触发条件 | 处理方式 | 用户提示 |
|---------|---------|---------|---------|
| 输入文件不存在 | `.output/feature-input/` 无文件 | 停止并提示 | "请先运行 feature-parser" |
| 格式不合法 | 必填字段为空 | 记录警告，跳过该条目 | "⚠️ SR-XXX 字段不完整，已跳过" |
| 工具不可用 | MCP/PICT 未配置 | 自动回退到备用方案 | "MCP 未配置，已回退 Web 搜索" |

---

## 6. 测试设计

| 测试场景 | 前置条件 | 操作 | 预期结果 | 验证方式 |
|---------|---------|------|---------|---------|
| 正常路径 | 输入文件格式正确 | 执行 Skill | 输出文件符合格式规范 | 人工检查输出 |
| 边界：空输入 | 输入文件存在但内容为空 | 执行 Skill | 提示"输入为空"，不创建输出文件 | 人工检查错误提示 |
| 边界：格式异常 | 输入含非法字符 | 执行 Skill | 跳过异常行并记录警告 | 检查 warning 输出 |

---

## 7. 实施步骤（TASK 粒度）

> 严格使用 TASK-ID + 确定性动词（创建/修改/删除）。

| TASK-ID | 动作 | 目标文件 | 详细描述 |
|---------|------|---------|---------|
| TASK-{id}-01 | 创建 | `.output/skills/<name>/SKILL.md` | 编写完整 Skill 定义，含 Frontmatter + 执行流程 |
| TASK-{id}-02 | 修改 | `.output/agents/ptm-tde.md` | 在 Skill 触发词映射表中注册新 Skill |
| TASK-{id}-03 | 创建 | `.output/scripts/<tool>.py` | 实现辅助工具脚本（如适用） |

---

## 8. 风险与回滚

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|---------|
| Skill 触发词与已有 Skill 冲突 | 低 | 中 | 实现前检查 ptm-tde.md 映射表 |
| 第三方工具（PICT/MCP）不可用 | 中 | 低 | 实现降级回退逻辑 |

**回滚方案**：删除新建文件，恢复 ptm-tde.md 映射表到修改前版本。

---

## 9. 人工确认区

> **元工作流检查点 ④ — Story LLD 确认**
> meta-po 发起，用户确认后方可进入实现。

**确认选项**：
1. ✅ **批准** — LLD 设计合理，允许进入 `TASK-{id}-01` 开始实现
2. ✏️ **需要修改** — 请指出具体修改点，meta-dev 更新后重新提交
3. ❌ **拒绝** — 设计方向有根本问题，需重新设计

**当前状态**：`draft` / `ready-for-lld-review` / `lld-approved`

---

<!-- meta-dev 填写：实现完成后追加以下内容 -->

## 10. 实现摘要（meta-dev 填写）

> 实现完成后由 meta-dev 回填，供 meta-qa 验证使用。

| 项目 | 计划 | 实际 |
|------|------|------|
| 创建文件数 | N | — |
| 修改文件数 | M | — |
| 偏差说明 | — | — |
